import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2a0c4e',
    },
    secondary: {
      main: '#f5f8de',
    },
    error: {
      main: '#9e2b25',
    },
  },
});

export default theme;